path: /content/animal_classification  # chemin d'accès à la racine des données
train: train  # dossier contenant les données d'entraînement
val: val  # dossier contenant les données de validation
nc: 3  # nombre de classes
names: ['cat', 'bird', 'dog']  # noms des classes
